global:
  scrape_interval: 15s
scrape_configs:
  - job_name: "spring-actuator"
    metrics_path: "/actuator/prometheus"
    static_configs:
      # バックエンドアプリがlocalで起動している場合： Prometheusコンテナからホストにアクセスする
      - targets: [ 'host.docker.internal:8081' ]